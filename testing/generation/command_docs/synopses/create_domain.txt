CREATE DOMAIN $name$ [ AS ] $data_type$
    [ COLLATE $collation$ ]
    [ DEFAULT $expression$ ]
    [ $constraint$ [ ... ] ]

where $constraint$ is:

[ CONSTRAINT $constraint_name$ ] { NOT NULL | NULL | CHECK ($expression$) }
